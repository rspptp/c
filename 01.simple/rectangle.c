#include<stdio.h>
main()
	{
		int x,y;
		float area,perimeter;
		printf("Enter the values of x and y\n");
		scanf("%f %f",&area,&perimeter);
		area=x*y;
		perimeter=2*(x+y);
		printf("%d %d is:%f\n",area);
		printf("%d %d is:%f\n",perimeter);
	}
